%h2= @pin.title
%p= @pin.description

Submitted by
= @pin.user.email

%br

= link_to "Back", root_path
= link_to "Edit", edit_pin_path
= link_to "Delete", pin_path, method: :delete, data: {confirm: "Do you really want to delete this pin?"}
