-# Offset increase the left margin of a column by * columns
.col-md-6.col-md-offset-3
  %h1 Edit Pin
  = image_tag @pin.image.url(:medium)
  = render 'form'
  %br
  = link_to "Cancel", @pin, :class =>  "btn btn-danger"
